package steps;

import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.edge.EdgeDriver;

import java.time.Duration;

public class LoginSteps {
    /*
    public WebDriver driver;
    @Given("user is navigated to Adjutor application")
    public void user_is_navigated_to_adjutor_application() {
        driver = new ChromeDriver();
        driver.get("https://app.adjutor.io/login");
        driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));

    }
    @When("user enter a valid email and password")
    public void user_enter_a_valid_email_and_password() {
        driver.findElement(By.id("email")).sendKeys("multititech11@gmail.com");
        driver.findElement(By.id("password")).sendKeys("Mtech@11");
        driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
    }
    @When("user click on login button")
    public void user_click_on_login_button() {
        driver.findElement(By.id("auth-button primary-btn-color filled-btn")).click();
        throw new io.cucumber.java.PendingException();
    }
    @Then("user is successfully logged in")
    public void user_is_successfully_logged_in() {
        // Write code here that turns the phrase above into concrete actions
        throw new io.cucumber.java.PendingException();
    }

     */

}
